# Zuerst wird das target auf sdkconfig_debug, oder sdkconfig_release gesetzt!
# generated by esp32-scripts
include(target.cmake) 


# The following lines of boilerplate have to be in your project's CMakeLists
# in this exact order for cmake to work correctly
cmake_minimum_required(VERSION 3.5)
list(APPEND EXTRA_COMPONENT_DIRS    "components/esp32-edc-toolkit"
                                    "components/esp32-edc-toolkit/protocols"
                                    "components/esp32-edc-azure"
                                    "main/airfit")
set (CMAKE_CXX_STANDARD 17)
set(IDF_TARGET esp32s3)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
set(EXCLUDE_COMPONENTS freemodbus)
project(app)
